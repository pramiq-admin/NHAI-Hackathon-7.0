<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy for the NHAI app.

  RELEASE builds (config-base): cleartext is DISALLOWED globally. Every
  outbound request must be HTTPS. This is the production posture.

  DEBUG builds (debug-overrides): cleartext is allowed ONLY for the
  loopback hosts used by the Metro bundler and the dev Android-emulator
  → host-loopback bridge (10.0.2.2). This lets `npx react-native
  run-android` work against a local FastAPI on `http://10.0.2.2:8000`
  without weakening release builds.

  See: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator's loopback to the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Genymotion / device-USB-reverse loopback equivalents. -->
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
